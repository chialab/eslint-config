const base = require('./base');
const javascript = require('./javascript');
const typescript = require('./typescript');

module.exports = {
    configs: {
        base,
        javascript,
        typescript,
    },
};
